Change the self.textinput.surface's rect to be the original lyric's rect to avoid the misalignment due to apostrophes?

Add scrolling capability

Scale all text based on window size using pygame.transform?

Prevent user from typing capital letters (it messes up the text offset)

Done:

Offset typed lyrics a bit to the left to give the text a shadow - 4/11/25

Convert old JSON to new JSON format.

Rewrite lyric display method for new JSON format.
